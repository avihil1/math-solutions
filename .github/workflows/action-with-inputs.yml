name: Mixed inputs

on:
  workflow_dispatch:
    inputs:
      name:
        type: choice
        description: Who to greet
        options: 
        - monalisa
        - avihil1
      message:
        required: true
      use-emoji:
        type: boolean
        description: Include 🎉🤣 emojis
      environment:
        type: choice
        options:
          - dev
          - prod
        default: dev

jobs:
  meet:
    runs-on: ubuntu-latest
    steps:
    - name: "Test input from other runner"
      run: echo "mingling" 
      outputs:
        stam:
          description: "input for next step"
          type: string
          
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: ${{ meet.outputs.stam }} Send greeting
      run: echo "${{ github.event.inputs.message }} ${{ fromJSON('["", "🥳"]')[github.event.inputs.use-emoji == 'true'] }} ${{ github.event.inputs.name }}"
