name: Mixed inputs

on:
  workflow_dispatch:
    inputs:
      name:
        type: choice
        description: Who to greet
        options: 
        - avihil1
        - monalisa
      message:
        required: true
      use-emoji:
        type: boolean
        description: Include 🎉🤣 emojis
      environment:
        type: choice
        options:
          - dev
          - prod
        default: dev

jobs:          
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: "Test input from other runner"
      id: step1
      run: echo "randid=$(echo Mingling)" >> $GITHUB_OUTPUT
      
    - name: ${{ join(steps.greet.outputs.randid, '\n') }} Send greeting
      id: step2
      run: echo "${{ steps.greet.outputs.randid }} ${{ fromJSON('["", "🥳"]')[github.event.inputs.use-emoji == 'true'] }} ${{ github.event.inputs.name }}"
